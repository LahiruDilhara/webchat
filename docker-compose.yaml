services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: testWebChat
      POSTGRES_DB: webchatTest
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - "8040:8080"